{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/shararamirez/budget/personal-budget-2026 && git add -A && { git diff --cached --quiet && exit 0; git commit -m 'Auto-save changes' && git push; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Auto-saving to GitHub..."
          }
        ]
      }
    ]
  }
}
